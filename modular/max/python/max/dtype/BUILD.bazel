load("//bazel:api.bzl", "modular_py_library", "requirement")

package(default_visibility = ["//max:consumers"])

modular_py_library(
    name = "dtype",
    srcs = glob(["**/*.py"]),
    ignore_unresolved_imports = [
        # Torch helpers are only defined if torch is installed.
        "torch",
    ],
    deps = [
        "//max/python/max:_core",
        requirement("numpy"),
    ],
)
