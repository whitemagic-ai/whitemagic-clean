[build-system]
requires = ["maturin>=1.4"]
build-backend = "maturin"

[project]
name = "whitemagic-rust"
version = "18.1.0"
description = "WhiteMagic Rust accelerators"
requires-python = ">=3.11"

[tool.maturin]
features = ["pyo3/extension-module"]
